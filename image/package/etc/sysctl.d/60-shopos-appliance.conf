# Keep the appliance responsive under memory pressure without pushing active
# database and PHP pages out to swap too eagerly.
vm.swappiness=10
vm.vfs_cache_pressure=75

# Bound dirty memory so a small Raspberry Pi does not accumulate a large burst
# of pending writes before flushing to USB storage.
vm.dirty_background_ratio=5
vm.dirty_ratio=15
