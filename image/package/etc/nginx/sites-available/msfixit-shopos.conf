server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;
    root /srv/www/wordpress;
    index index.php index.html;

    client_max_body_size 128m;
    server_tokens off;

    location = /shopos-health {
        default_type text/plain;
        alias /run/msfixit-shopos/health.txt;
        add_header Cache-Control "no-store" always;
    }

    include snippets/msfixit-admin-console.conf;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # Regex locations are evaluated in declaration order. Block executable PHP
    # in writable WordPress areas before the generic PHP-FPM handler can match.
    location ~* /(?:uploads|files)/.*\.php$ {
        deny all;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/msfixit-fpm.sock;
        fastcgi_read_timeout 120s;
        fastcgi_param HTTPS $shopos_fastcgi_https;
        fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
    }

    location ~ /\. {
        deny all;
    }
}
