# Translate Cloudflare's forwarded scheme into the value WordPress expects in
# the CGI HTTPS variable. Local plain-HTTP access remains marked as off.
map $http_x_forwarded_proto $shopos_fastcgi_https {
    default off;
    https on;
}
